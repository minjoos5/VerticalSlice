using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using Unity.VisualScripting;

public class CassetteManager : MonoBehaviour
{
/*
    [SerializeField] public List <GameObject> _cassList = new List <GameObject> {};

    [SerializeField] public List <GameObject> _cassLocation = new List <GameObject> {};

    public List <GameObject> _temp = new List <GameObject> {};

    public Cassette _cassette;

    void Awake()
    {
        _cassette.CassettePrefab();

        _temp = _cassLocation.OrderBy( x => Random.value ).ToList( );

        for (int i = 0; i < _cassLocation.Count; i++)
        {
            _cassList[i].transform.position = _temp[i].transform.position;
            _cassList[i].SetActive(true);
        }
    }

    void Start()
    {
        
    }*/
}
